meta {
  name: 3 salvar usuario ADMIN
  type: http
  seq: 4
}

post {
  url: http://localhost:8080/api/usuarios
  body: json
  auth: inherit
}

body:json {
  {
    "username": "joao.admin",
    "password": "123",
    "roles": ["ADMIN", "USER"]
  }
  
}

settings {
  encodeUrl: true
  timeout: 0
}
