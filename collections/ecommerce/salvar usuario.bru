meta {
  name: salvar usuario
  type: http
  seq: 6
}

post {
  url: http://localhost:8080/api/usuarios
  body: json
  auth: inherit
}

body:json {
  {
    "username": "joao.silva",
    "password": "123",
    "roles": ["ADMIN", "USER"]
  }
  
}

settings {
  encodeUrl: true
}
