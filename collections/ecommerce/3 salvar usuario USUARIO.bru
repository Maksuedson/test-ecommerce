meta {
  name: 3 salvar usuario USUARIO
  type: http
  seq: 5
}

post {
  url: http://localhost:8080/api/usuarios
  body: json
  auth: inherit
}

body:json {
  {
    "username": "joao.usuario",
    "password": "123",
    "roles": ["USER"]
  }
  
}

settings {
  encodeUrl: true
  timeout: 0
}
