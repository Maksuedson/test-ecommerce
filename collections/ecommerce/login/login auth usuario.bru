meta {
  name: login auth usuario
  type: http
  seq: 3
}

post {
  url: http://localhost:8080/api/login
  body: json
  auth: inherit
}

body:json {
  {
    "login": "joao.usuario",
    "password": "123"
  }
}

tests {
  pm.environment.set("token", pm.response.text())
}

settings {
  encodeUrl: true
  timeout: 0
}
