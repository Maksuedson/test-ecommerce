meta {
  name: login auth
  type: http
  seq: 2
}

post {
  url: http://localhost:8080/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "username": "joao.silva",
    "password": "123"
  }
}

settings {
  encodeUrl: true
}
