meta {
  name: login
  type: http
  seq: 1
}

post {
  url: http://localhost:8080/login
  body: json
  auth: inherit
}

body:json {
  {
    "login": "joao.silva",
    "senha": "123"
  }
}

settings {
  encodeUrl: true
}
