meta {
  name: login
  type: http
  seq: 1
}

get {
  url: http://localhost:8080/login
  body: json
  auth: inherit
}

body:json {
  {
    "login": "usuario",
    "senha": "123"
  }
}

settings {
  encodeUrl: true
}
