meta {
  name: login auth admin
  type: http
  seq: 2
}

post {
  url: http://localhost:8080/api/login
  body: json
  auth: inherit
}

body:json {
  {
    "login": "joao.admin",
    "password": "123"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
